apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-deployment
  labels:
    app: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      # Service account with SCC binding
      serviceAccountName: three-tier-app-sa
      # Disable service account token mounting for security
      automountServiceAccountToken: false
      # Allow root access
      securityContext:
        runAsUser: 0  # Root user
        fsGroup: 0
        seccompProfile:
          type: RuntimeDefault
      containers:
      - name: backend
        image: mmsaeed509/go-app:v1
        # Container security context for root access
        securityContext:
          runAsUser: 0  # Root user
          seccompProfile:
            type: RuntimeDefault
        ports:
        - containerPort: 8000
          protocol: TCP
        env:
        - name: DB_HOST
          value: "db"
        - name: DB_PORT
          value: "3306"
        - name: DB_NAME
          value: "example"
        - name: DB_USER
          value: "appuser"
        volumeMounts:
        - name: db-password
          mountPath: /run/secrets
          readOnly: true
        # Health checks
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /ready
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 5
        # Resource limits
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "100m"
      volumes:
      - name: db-password
        secret:
          secretName: db-secret
          items:
          - key: password
            path: db-password